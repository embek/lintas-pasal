<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Lintas Pasal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="d-flex flex-column min-vh-100">

<div class="flex-grow-1">
    <div class="container-fluid px-0">
        <div class="row justify-content-center align-items-center mx-0 min-vh-85">
            <div class="col-12 col-md-8 col-lg-6">
                <div class="glass-card text-center p-5">
                    <div class="error-icon mb-4">
                        <i class="fas fa-exclamation-triangle text-warning error-icon-large"></i>
                    </div>

                    <h1 class="display-4 mb-3 text-wiki-dark">Error <%= error.status || 500 %></h1>
                    <p class="lead mb-4 text-wiki-secondary">
                        <% if (error.status === 404) { %>
                            Halaman yang Anda cari tidak ditemukan
                        <% } else { %>
                            <%= error.message || 'Terjadi kesalahan pada server' %>
                        <% } %>
                    </p>

                    <div class="row g-3 justify-content-center">
                        <div class="col-auto">
                            <a href="/" class="btn btn-gradient btn-lg">
                                <i class="fas fa-home me-2"></i>Kembali ke Beranda
                            </a>
                        </div>
                        <div class="col-auto">
                            <button onclick="history.back()" class="btn btn-outline-light btn-lg">
                                <i class="fas fa-arrow-left me-2"></i>Halaman Sebelumnya
                            </button>
                        </div>
                    </div>

                    <% if (process.env.NODE_ENV === 'development') { %>
                    <details class="mt-4 p-3 bg-dark rounded text-start">
                        <summary class="text-warning cursor-pointer">Debug Info (Development Mode)</summary>
                        <pre class="text-light mt-2 small"><%= error.stack %></pre>
                    </details>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer', { footerClass: 'mt-auto w-100 text-center py-3' }) %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Lintas Pasal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="d-flex flex-column min-vh-100">

<div class="flex-grow-1">
<div class="container-fluid px-0">
    <div class="row justify-content-center align-items-center mx-0" style="min-height: 85vh;">
        <div class="col-12 col-md-8 col-lg-6">
            <div class="glass-card text-center p-5">
                <div class="error-icon mb-4">
                    <i class="fas fa-exclamation-triangle text-warning" style="font-size: 5rem;"></i>
                </div>
                
                <h1 class="display-4 mb-3 text-wiki-dark">Error <%= error.status || 500 %></h1>
                <p class="lead mb-4 text-wiki-secondary">
                    <% if (error.status === 404) { %>
                        Halaman yang Anda cari tidak ditemukan
                    <% } else { %>
                        <%= error.message || 'Terjadi kesalahan pada server' %>
                    <% } %>
                </p>
                
                <div class="row g-3 justify-content-center">
                    <div class="col-auto">
                        <a href="/" class="btn btn-gradient btn-lg">
                            <i class="fas fa-home me-2"></i>Kembali ke Beranda
                        </a>
                    </div>
                    <div class="col-auto">
                        <button onclick="history.back()" class="btn btn-outline-light btn-lg">
                            <i class="fas fa-arrow-left me-2"></i>Halaman Sebelumnya
                        </button>
                    </div>
                </div>
                
                <% if (process.env.NODE_ENV === 'development') { %>
                <details class="mt-4 p-3 bg-dark rounded text-start">
                    <summary class="text-warning cursor-pointer">Debug Info (Development Mode)</summary>
                    <pre class="text-light mt-2 small"><%= error.stack %></pre>
                </details>
                <% } %>
            </div>
        </div>
    </div>
</div>
